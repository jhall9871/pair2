export type FoodSubtype = {
    iconUrl: string,
    name: string,
    pairingArray: number[],
    subtypes: string[]
}

export type WineSubtype = {
    bottleType: string,
    name: string,
    pairingArray: number[],
    subtypes: string[]
}